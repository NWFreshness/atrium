# Neon Postgres connection string (postgresql://...)
DATABASE_URL=
# AUTH_SECRET is required for Auth.js JWT sessions.
# Generate with: openssl rand -base64 32
AUTH_SECRET=
AUTH_OWNER_EMAIL=
AUTH_OWNER_PASSWORD=
AUTH_DEMO_EMAIL=
AUTH_DEMO_PASSWORD=
# Phase 6: only the literal string "true" opens /signup. Anything else is closed.
AUTH_SIGNUP_ENABLED=
