# Feature 9.4: Auth attempt throttle

**Phase:** 9 — Security
**Status:** completed
**Estimated effort:** 3–4 hours
**Depends on:** 9.3
**Blocks:** nothing

## Goal

Five failed logins (or signups) for the same mailbox in 15 minutes, or twenty from the same IP, fail closed for the rest of that window. Copy stays generic. Phase 6’s named residual (“bots can open empty tenants”) is closed without a mailer.

## Scope

Include:

- `lib/auth/throttle.ts` — policy: subjects, limits, window, `assertNotThrottled` / `recordFailure` / `clearFailures`. Returns `{ ok: true } | { ok: false }`. Throws nothing. Imports neither `SignUpError` nor login types
- Memory repository for unit tests; Drizzle repository for production
- Table `auth_throttles` (no `tenantId`, not demo-reset, not in the 7.3 tenant-index list)
- Generated migration; read it; `IF NOT EXISTS`; no RLS
- `authorizeCredentials` consults the throttle **after** empty-field checks and **before** `findByEmail` / `verify`. A throttled subject returns `null` without bcrypt. A failed verify calls `recordFailure`. A successful auth calls `clearFailures` for that email (and IP if present)
- `signUp` consults the same helper **after** the flag check and **before** `findByEmail`. Throttled → `SignUpError("unavailable")`. Failed create (including duplicate) records a failure. Successful create clears
- Injectable clock and store so `env -u DATABASE_URL npm test` covers allow, deny, window expiry, success-clears

Exclude (for now):

- CAPTCHA, mailer, MFA
- Distinct “too many attempts” copy
- Logging plaintext email or IP
- Throttling authenticated app POSTs (import is 9.5)
- Reset-demo / Groove
- Renaming `middleware.ts`

## Files to create

- `lib/auth/throttle.ts`
- `lib/auth/throttle.test.ts`
- `lib/auth/throttle-memory.ts` and/or keep the memory impl in `throttle.ts` if it stays under ~120 lines
- `lib/auth/throttle-drizzle.ts` — only this file imports `../db`
- drizzle migration files drizzle-kit generates

## Files to modify

- `lib/db/schema.ts` — `authThrottles` table
- `lib/db/schema.test.ts` — table exists, **no** `tenantId`, unique on `subject`; the 7.3 derived tenant-index set must **not** include it
- `lib/auth/authorize.ts` — wire throttle (deps argument, default production store lazy)
- `lib/auth/authorize.test.ts` — throttled subject skips `verify`; fifth failure then blocks
- `lib/auth/signup.ts` / `signup.test.ts` — same policy, `unavailable`
- `auth.ts` — pass request IP into authorize when NextAuth provides `request` as the second argument; if this Auth.js beta does not, email-only is the production path and the spec’s IP tests stay on the helper. Record which way the antecedent resolved in Shipped notes
- `.env.example` — no new names

## Spec

1. Constants (one definition): `EMAIL_FAILURE_LIMIT = 5`, `IP_FAILURE_LIMIT = 20`, `WINDOW_MS = 15 * 60 * 1000`.
2. Subjects: `email:${lowerTrimmedEmail}` and, when `ip` is a non-empty string, `ip:${ip}`. Do not store the raw email column; the subject string is the key.
3. Window: first failure inserts `failedCount = 1`, `windowStartedAt = now`. Later failures in the same window increment. `now - windowStartedAt >= WINDOW_MS` starts a new window at count 1.
4. `assertNotThrottled`: if email subject count `>= 5` or ip subject count `>= 20` inside the window → `{ ok: false }`. Otherwise `{ ok: true }`.
5. `clearFailures` deletes both subjects for that attempt.
6. Login: throttled → `null`, same as wrong password. Do not call `verify`.
7. Signup: throttled → `SignUpError("unavailable")` (generic, same as duplicate). Flag-closed still wins first (`closed`).
8. Schema: `auth_throttles` columns `id` uuid pk, `subject` text not null unique, `failedCount` integer not null, `windowStartedAt` timestamp not null. No FK to users. No `tenantId`.
9. `schema.test.ts` tenant-index derived list still equals the tables that have `tenantId`. Adding this table must not fail 7.3 — that is the proof it is unscoped on purpose.
10. Groove still has no tables. Demo reset does not touch `auth_throttles`.
11. IP is taken from `x-forwarded-for` first hop **only if** Auth.js hands the request in. Spoofed IP is residual behind Vercel; do not add a new env trust list.

## Acceptance criteria

1. [x] `auth_throttles` exists, has no `tenantId`, unique on `subject`
2. [x] 7.3’s tenant-index test still passes without listing this table
3. [x] Five failures for one email inside 15 minutes: sixth `assertNotThrottled` is `{ ok: false }`; after 15 minutes it is `{ ok: true }`
4. [x] Twenty failures for one IP: twenty-first denied; a different IP is unaffected
5. [x] Success `clearFailures` lets the next attempt through
6. [x] `authorizeCredentials` on a throttled email does not call `verify`
7. [x] `signUp` on a throttled email throws `SignUpError("unavailable")` and does not insert
8. [x] Generated migration is additive (CREATE TABLE / unique index), idempotent `IF NOT EXISTS`, no DROP of users/tenants
9. [x] `env -u DATABASE_URL npm test` and `AUTH_SECRET=ci-build-placeholder npm run build` pass
10. [x] No new runtime dependencies (no Redis, no Upstash)

## Implementation notes

- Lazy Drizzle store: do not `getDb()` at module load (signup’s fail-closed lesson).
- `db.batch` is not required for a single upsert; one insert/update is enough. If you upsert, do not use `on conflict (subject)` against a unique that is not that column — 8.1’s 42P10 lesson.
- After merge: `npm run db:migrate` on the Neon **dev** URL. Do not paste the connection string.
- Do not run prettier on a directory.
- Unit tests prove the policy; Neon’s unique and the live increment are untested until migrate (same honesty as 7.2). Say so in Shipped notes.

## Verification

```bash
env -u DATABASE_URL npm test
AUTH_SECRET=ci-build-placeholder npm run build
```

No Playwright. Do not add live stuffing requests.

## Commit

`fix(auth): throttle repeated login and signup failures`

## Shipped notes (2026-09-11)

`auth_throttles` is unscoped (no `tenantId`, not in 7.3’s tenant-index list, not demo-reset). Unique is the column `subject` (`email:` + lowercased mailbox, or `ip:` + address). Policy lives in `lib/auth/throttle.ts` (no SignUpError, no login types). Memory store for tests; Drizzle store is select-then-insert/update (no `on conflict`) and `createLazyDrizzleThrottleStore` does not call `getDb` at module load.

Limits: 5 / 15 min per email, 20 / 15 min per IP. Login consults the throttle after empty-field checks and before `findByEmail`/`verify`; a blocked subject returns `null` without bcrypt. Signup checks the flag first, then the throttle, then lookup; blocked → `SignUpError("unavailable")` with no insert. Closed still wins over throttle.

**Auth.js does pass `request` as the second argument to Credentials `authorize`.** Production login takes the first `x-forwarded-for` hop via `requestIp`. Unit IP tests stay on the helper as well.

`drizzle/0007_jittery_retro_girl.sql` is `CREATE TABLE IF NOT EXISTS` plus `UNIQUE("subject")`. `env -u DATABASE_URL npm run db:generate` reports no drift. Neon unique/increment is untested until `npm run db:migrate` on the dev URL after merge — unit tests prove the policy only.

Verified: `env -u DATABASE_URL npm test` 679 passed (105 files, up from 668/104); `AUTH_SECRET=ci-build-placeholder npm run build` exit 0. No Playwright. No new runtime dependencies. I have not run `db:migrate` (post-merge).

