# Feature 10.6: Groove instrument recast

**Phase:** 10 — PNW (redesign)
**Status:** specced
**Estimated effort:** 2–4 hours
**Depends on:** 10.1
**Blocks:** 10.7

## Goal

The groovebox is recast as a piece of dark anodised hardware that belongs to the same product as the rest of the theme — basalt chassis, lichen meters, cedar for overload, golden for the lit play LED — while keeping its own instrument token set and staying dark under `data-theme="light"`.

## Scope

Include:

- `components/groove/groove-shell.module.css`: replace the 14 espresso-era `--inst-*` values with the PNW instrument set (table below). The light-theme overrides **stay absent** — this desk does not re-skin for paper, and 5.6 removed those overrides deliberately
- Chassis, nameplate, corner screws, transport bar, four unit cards (`RHYTHM DR-16`, `BASS MB-1`, `PADS PX-4`, `LEAD LX-2`), master strip, scope canvas borders
- `components/groove/transport.tsx` play LED: lit = `--inst-brass` (golden), unlit = `--inst-line`; the STOP/PLAY labels and aria states unchanged
- `components/groove/master.tsx`, `scope.tsx`: knob arcs in `--inst-ink`, sweep meter in `--inst-brass` → `--inst-clay` above the overload threshold, scope trace in `--inst-moss`
- Keep every bare `groove-*` DOM class as a literal via `:global()`; keep the four units' equal heights (`align-items: stretch` + `margin-top: auto`)

| Token                 | Espresso era           | PNW                    |
| --------------------- | ---------------------- | ---------------------- |
| `--inst-deep`         | `#0c0a07`              | `#14181a`              |
| `--inst-bg`           | `#131009`              | `#1b1f21`              |
| `--inst-panel`        | `#1a1510`              | `#23282a`              |
| `--inst-panel-hi`     | `#231d14`              | `#2c3234`              |
| `--inst-line`         | `#3a3325`              | `#3c4344`              |
| `--inst-line-soft`    | `#2b2419`              | `#2b3133`              |
| `--inst-ink`          | `#f4ecdc`              | `#eef1f2`              |
| `--inst-dim`          | `#ab9c81`              | `#b3bfa9`              |
| `--inst-faint`        | `#7d7159`              | `#a3b19b`              |
| `--inst-brass`        | `#dfa33c`              | `#dfa84a`              |
| `--inst-brass-bright` | `#f3c168`              | `#efc272`              |
| `--inst-brass-deep`   | `#7d5713`              | `#8a6320`              |
| `--inst-brass-glow`   | `rgba(223,163,60,.55)` | `rgba(223,168,74,.55)` |
| `--inst-moss`         | `#8fae83`              | `#a3b19b`              |
| `--inst-clay`         | `#cd7258`              | `#b87355`              |

Exclude (for now):

- Any audio engine, scheduling, patch, or parameter change (`lib/groove/audio/**`, `lib/groove/patches.ts` untouched)
- Unit names, patch names (A · NEON RIVIERA, B · BASALT, C · SUNROOM, D · LATE ORBIT), or the four-unit layout
- Making Groove theme-aware. It stays instrument-dark on purpose; the light theme does not reach it
- Adding a Groove table, seed, or resetter (AGENTS.md forbids it)

## Files to create

- `components/groove/groove-pnw.test.ts` — extends `groove-pass.test.ts`: the PNW instrument values, the continued absence of `[data-theme="light"]` overrides in the stripped ruleset, `:global` class contract, chassis/screws/units

## Files to modify

- `components/groove/groove-shell.module.css` (token block + restyle)
- `components/groove/transport.tsx`, `master.tsx`, `scope.tsx` (class hooks only)
- `components/groove/groove-pass.test.ts` (instrument assertions move to the PNW values)
- `e2e/workroom.spec.ts` — the Groove instrument-token assertions (`--inst-panel: #1a1510` → `#23282a`, `--inst-ink: #f4ecdc` → `#eef1f2`, and the computed `color` `rgb(244,236,220)` → `rgb(238,241,242)`)

## Spec

1. The instrument set is a nested scope, not a re-skin of the app: `--inst-*` live in `groove-shell.module.css` and never fall back to `--bg-*`/`--ink*`. A missing instrument token must be a visible failure in review, not a silent inheritance from the PNW app theme.
2. The desk stays dark under `data-theme="light"`: no `[data-theme="light"]` block in the instrument ruleset. 10.7's namespace gate asserts the absence explicitly (5.6 built that assertion; this feature must not weaken it).
3. Bare `groove-*` class names stay literal via `:global()`. `e2e/groove.spec.ts` selects `.groove-master-leds .groove-led` and tests `className.includes("on")`, so hashing these names silently breaks the smoke — 5.6 shipped that bug once.
4. The lit/unlit LED contrast is the one measured pair inside Groove: `--inst-brass #dfa84a` against `--inst-panel #23282a` must stay ≥3:1 (it measures 5.6:1), and the unlit state must remain distinguishable by shape, not only colour (the LED keeps its inset ring).
5. Warmth moves from brass-orange to PNW golden/cedar; the chassis goes cool (basalt) rather than warm-brown. Nothing else about the desk changes: same dimensions, same unit heights, same playhead behaviour.
6. `e2e/groove.spec.ts` (8 tests) must pass unchanged except for any assertion that hardcodes an instrument value — and the only such assertions are the two in `e2e/workroom.spec.ts` named above, which this feature updates in the same commit.

## Acceptance criteria

1. [ ] All 15 `--inst-*` tokens carry the PNW values; no espresso-era instrument hex remains in `groove-shell.module.css`
2. [ ] No `[data-theme="light"]` override exists in the instrument ruleset; the desk renders dark under both themes
3. [ ] Bare `groove-*` names remain literal (`:global`), and `.groove-master-leds .groove-led` still resolves with an `on` class while playing
4. [ ] `e2e/groove.spec.ts` 8 tests green; `e2e/workroom.spec.ts` Groove assertions updated and green
5. [ ] Page height stays in the same range as the 5.6 baseline (~1,300px at 1280px wide); no unit card collapses or overflows
6. [ ] `env -u DATABASE_URL npm test`, `AUTH_SECRET=ci-build-placeholder npm run build` green

## Implementation notes

- Groove's CSS has no theme variables of its own beyond `--inst-*`; a search-and-replace of the 15 values plus the transit/master/scope class hooks is most of this feature.
- Run the Groove smoke headed once (audio is audible only in a headed run) but keep the assertions in headless CI; stop the transport in `finally` as the existing spec does.
- If a knob arc or the scope trace goes low-contrast against the new panel, fix the instrument value — not the app tokens. The desk is allowed to be its own world; it is not allowed to be unreadable.

## Verification

```bash
env -u DATABASE_URL npm test
AUTH_SECRET=ci-build-placeholder npm run build
npx playwright test e2e/groove.spec.ts e2e/workroom.spec.ts
```

## Commit

`feat(pnw): Groove instrument recast`
