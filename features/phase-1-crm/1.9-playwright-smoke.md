# Feature 1.9: CRM Playwright smoke

**Phase:** 1 — CRM
**Status:** completed
**Estimated effort:** 3–4 hours
**Depends on:** 1.8
**Blocks:** nothing

## Goal

CI can prove a logged-in demo user can walk the CRM without 404s or mixed-tenant data.

## Scope

Include:
- `e2e/crm.spec.ts` (or similar)
- Demo login → `/crm` dashboard renders
- Five subnav sections open
- Organizations table shows seed rows
- One write path: create or edit an org (or move a deal) and still see it after reload
- Unauthenticated `/crm` → `/login`
- Owner login: CRM loads empty of demo seed names (no demo leakage)
- CI already runs `npx playwright test`; do not hardcode only `login.spec.ts`

Exclude (for now):
- Full Bench exploratory checklist
- Visual screenshot diffs
- Space / Rolodex / Groove e2e

## Files to create

- `e2e/crm.spec.ts`

## Files to modify

- None required in CI if it already runs the full Playwright suite
- Optional: `data-testid`s on pipeline totals if the smoke asserts them

## Acceptance criteria

1. [x] Playwright CRM smoke passes locally with `.env` secrets
2. [x] Unauthenticated `/crm` redirects to login
3. [x] Demo sees seed CRM data; owner does not see those demo names
4. [x] `npm test` and `npm run build` still pass
5. [x] Existing login/launcher/theme e2e still pass
6. [x] No regressions in 1.8

## Implementation notes

- Use `AUTH_DEMO_*` for the happy path; skip if missing (same pattern as demo login test).
- Prefer role/label selectors over brittle CSS.
- Keyboard pipeline drag is optional here if 1.6 unit tests already cover moveDeal.

## Verification

```bash
env -u DATABASE_URL npm test
AUTH_SECRET=ci-build-placeholder npm run build
npx playwright test
```

Expected: previous e2e + new CRM smoke green.

## Commit

`test(e2e): CRM smoke for demo and owner isolation`
