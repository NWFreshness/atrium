# Feature 1.5: Deals table

**Phase:** 1 — CRM
**Status:** pending
**Estimated effort:** 3–4 hours
**Depends on:** 1.4
**Blocks:** 1.6, 1.7

## Goal

Users can add, search, edit, and delete deals with stage, value (USD), probability, close date, organization, and primary contact.

## Scope

Include:
- Deals table (TanStack Table)
- Search by deal name (and org/contact names, matching Bench)
- Add / edit / delete
- Default probability from `STAGE_PROBABILITY` when stage is set and probability omitted
- Explicit probability on the form is kept until stage changes (stage change rebase is 1.6 `moveDeal`; form stage change should rebase the same way)
- New deal `boardOrder` = end of that stage column
- Organization detail lists its deals
- Tenant-scoped actions + unit tests

Exclude (for now):
- Pipeline board and drag-and-drop (1.6)
- Dashboard (1.8)

## Files to create

- `app/(authenticated)/crm/deals/[id]/page.tsx`
- `components/crm/deal-table.tsx`
- `components/crm/deal-form.tsx`
- `lib/crm/deal-actions.ts`
- `lib/crm/deal-*.test.ts`

## Files to modify

- `app/(authenticated)/crm/deals/page.tsx`
- `app/(authenticated)/crm/organizations/[id]/page.tsx` — list deals
- Contact detail may show related deals if cheap

## Spec

Stages in order: New, Qualified, Proposal, Negotiation, Won, Lost.
Value is USD number. Single currency.

## Acceptance criteria

1. [ ] Deals table lists this tenant’s deals with stage, value, close date, org, primary contact
2. [ ] Add, edit, delete persist across refresh
3. [ ] Search narrows the list
4. [ ] Default probability matches stage constants; tests cover it
5. [ ] Tenant isolation tests pass
6. [ ] `npm test` and `npm run build` pass
7. [ ] No regressions in 1.3–1.4

## Implementation notes

- Format money/dates through a small `lib/crm/format.ts` (or equivalent) used by table and later pipeline/dashboard.
- Do not add `@hello-pangea/dnd` here.

## Verification

```bash
env -u DATABASE_URL npm test
AUTH_SECRET=ci-build-placeholder npm run build
```

Manual: demo → Deals → seed rows across stages; edit value; refresh.

## Commit

`feat(crm): deals table CRUD`
